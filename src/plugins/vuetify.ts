// src/plugins/vuetify.ts
import "vuetify"; // Import Vuetify's default styles
import { createVuetify } from "vuetify";

export default createVuetify({
  theme: {
    defaultTheme: "light", // Light theme is the default, you can change it
  },
});
